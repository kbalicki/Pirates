# Sesja 2026-09-24 — v0.95.0.0 → v0.96.0.0

**Zakres liczy się z listy.** v0.84.0 pytało, czy każdy związany klawisz jest
nazwany. To wydanie pyta odwrotnie: czy legenda nie nazywa **więcej**, niż
lista ma wierszy.

## 1. Cyfry (TODO 7)

| ekran | przed | po |
|---|---|---|
| gubernator, 4 opcje | `1-9 — odpowiedź`, 5 martwych klawiszy | `1-4` |
| łupy, 3 wiersze | `1-4`, `4` związany z niczym | `1-3`, `4` niezwiązany |
| łupy, 5 wierszy (trzy listy kaperskie) | `1-4`, piąty tylko kursorem | `1-5` |

`{{digits}}` w tabeli locale, `digitRange(n)` i `DIGIT_KEYS` w
`core/services/legendKeys.ts`. Czytany niewypełniony `{{digits}}` obiecuje
`1`–`9` — najszerzej, jak da się go narysować. Łupy wiążą cyfry w `openSpoils`,
po jednej na wiersz. Sonda: łupy `NIC-NIE-ZMIENIA: ESC L` (było `4 ESC L`),
gubernator `RAZEM: 0`.

Równoległa sesja („Pirates (2)”) znalazła w tym samym czasie drugą połowę:
**samotna cyfra przed myślnikiem nie jest klawiszem** — czternaście wierszy
listy zoomu (`8 — Detale`) i `za 2–4× cenę` w podręczniku czytały się jako
obietnice. Zgłaszane były tylko 8 i 9, bo 1-7 kryła prawdziwa `1-7 — karta`.
Jej zmiana weszła w ten sam commit.

## 2. Zakładka zapisu (TODO 7a) — i trzy wady na tym samym ekranie

- `L — wczytaj` / `Delete/X — usuń` obiecywane na pustym slocie. Linia idzie
  za slotem pod kursorem. Legenda była też rysowana **dwa razy** (pod slotami
  i w stopce panelu) — została stopka.
- `Slot 1: Dzień Day 1` — tytuł zapisu to `Day N` po angielsku od pierwszego
  zapisu. `saveTitleDay()` wyciąga numer, ekran pisze po swojemu. Działa na
  starych zapisach.
- Lista wczytywania na ekranie tytułowym datowała zapis od domyślnej ery:
  gra z 1 stycznia **1680** jako **1690**. Czyta `world.startYear` zapisu.
- `[Usuń]` wystawał poza prawą krawędź panelu. Przyciski układane od krawędzi
  według zmierzonej szerokości, w tych samych kolumnach dla pustych i pełnych.

**Pułapka pomiaru:** pierwsza sonda „pokazała”, że `↑` na zakładce zapisu nie
działa. To IndexedDB — sloty są rysowane po asynchronicznym odczycie, a
`pump()` pompuje klatki, nie czas rzeczywisty. Z odstępami 250 ms wszystko
działa.

## 3. 8a zamknięte pomiarem ujemnym

`MainMapScene` przebudowuje się w stronie (3 przebudowy, `reloadOnly: []`),
`H` + jedna klatka → mapa już zapauzowana, usunięcie sceny z wiszącą pauzą →
zero wyjątków. Na starym i nowym kodzie tak samo. Poprawka napisana i cofnięta.

## 4. Stan

| | v0.95.0.0 | v0.96.0.0 |
|---|---|---|
| testy | 2433 | **2440** |
| pliki testowe | 88 | **89** |
| `audit-layout.mjs` | RAZEM: 0 | RAZEM: 0 |

Zero zmian w `WorldState`, zero migracji.

## 5. Nowe, nienaprawione

- Data po polsku w mianowniku: `1 Styczeń 1680` (HUD, lista zapisów) — powinno
  być `1 stycznia 1680`. `getMonthName` zwraca mianownik; potrzebny dopełniacz.

---

# v0.96.0.0 → v0.96.1.0 — pozycja 3 z TODO, pomiar

## `DEFENCE_FLOOR` a `PREY_AGGRESSION_FLOOR`

Dwie liczby o tej samej wartości 0,35. Pierwsza to **próg** na rzucie agresji,
druga — **udział wagi bojowej**, który wystawia kadłub bez woli walki. Co druga
rozstrzyga, na świeżych kadłubach z tabeli spawnu:

| podłoga | pary rover→kupiec przechodzące `PREY_ODDS` |
|---|---|
| 0 / 0,2 / **0,35** | 16 z 16 |
| 0,5 | 15 z 16 (pinasa odpada od brygantyny) |
| 0,7 | 12 z 16 (pinasa odpada od wszystkich) |
| 1,0 | 10 z 16 |

Najgorsza para przy 0,35 to 46% walki, a bramka zaczyna od ok. 41% — więc na
pełnym kadłubie **nie odmawia nigdy**; gryzie przez współczynnik kadłuba
w `fightingWeight`. Przypięte testem, zero zmian w grze.

## Złoto poza kopalnią — pomiar do decyzji

Zobacz TODO, pozycja 3: w 45 z 45 portów bez kopalni notowanie złota stoi na
`RATIO_MAX` (×3), bo `spotPrice` daje towarowi bez konsumenta fikcyjny popyt
1 t/dzień, a zapas złota jest tam zawsze 0. Kurs z 30 t ≈ +3 000. **Nie
ruszone** — to decyzja balansowa.

---

# v0.96.1.0 → v0.97.0.0 — liczby i data napisane po angielsku

Wzięte z TODO 8b (data w mianowniku) i **przemiecione do końca rodziny**, jak
każe metoda z v0.79.0: pozycja mówiła o dwóch miejscach, a chodziło o jedną
regułę złamaną w trzynastu.

## 1. Ustawienie, które kamera zabierała z powrotem

Luneta ma **czternaście stopni** na ekranie kwatermistrza i pod nimi linię
`* Działa natychmiast`. Wybór stopnia zapisywał ustawienie, a potem sam sięgał
do `MainMapScene` i wołał `cameras.main.setZoom` — a `CameraController.update()`,
który chodzi w każdej klatce, gdy czarta nie jest zapauzowana, **ściągał kamerę
z powrotem** do pola `zoomTarget` ustawionego raz w konstruktorze i ruszanego
wyłącznie kółkiem myszy.

Zmierzone w działającej grze — ustaw poziom, szturchnij kamerę tak, jak robił to
ekran, przepuść sekundę gry:

| wybrane | po szturchnięciu | po sekundzie |
|---|---|---|
| z1 (1,5×) | 1,5 | **6** |
| z3 (2,5×) | 2,5 | **6** |
| z8 (6×) | 6 | 6 |
| z14 (12×) | 12 | **6** |

**Trzynaście z czternastu ustawień nie robiło nic. Czternaste jest domyślne.**
Trzymało się, dopóki menu było otwarte, bo czarta stoi za nim zapauzowana — i to
jest dokładnie powód, dla którego wyglądało na działające.

**Drabiny były trzy.** Czternaście stopni ekranu (1,5 · 2 · 2,5 · 3 · 3,5 · 4 ·
5 … 12); kółko myszy chodzące po liczbach całkowitych (`Math.round(target) ± 1`,
podłoga `1`), które **nie umiało trafić w sześć półstopni poniżej 4×**, za to
umiało zejść do `1×`, które nie jest żadnym stopniem i jest szersze niż
deklarowane w podręczniku `1,5×`, i **nigdy nie zapisywało poziomu**, więc ekran
dalej pokazywał wiersz, z którego gracz już zszedł; oraz wskaźnik w rogu, który
zaokrąglał powiększenie i dawał **jedenaście odczytów na czternaście ustawień** —
1,5 i 2 czytały się jako `2`, 2,5 i 3 jako `3`, 3,5 i 4 jako `4`, a obiecywane
przez własny komentarz `1×` nie mogło się pojawić nigdy.

Teraz drabina jest jedna: `getZoomValue()`, czytane przez kamerę w każdej klatce.
Kółko robi jeden krok po tej samej liście i zapisuje poziom. Potwierdzone
w grze — sześć kliknięć „dalej” od domyślnego 6×:

```
6 → 5 → 4 → 3,5 → 3 → 2,5 → 2      pc_zoom_level: null → z2
```

Przed poprawką te same sześć kliknięć dawało `6 → 5 → 4 → 3 → 2 → 1 → 1`
i nie zapisywało nic.

`CameraController` przestał importować Phasera jako wartość (używał go wyłącznie
jako typu) — dzięki temu `cameraZoom.test.ts` prowadzi prawdziwy kontroler na
zwykłym obiekcie kamery, bez przeglądarki, i sprawdza **wszystkie czternaście**
stopni przez sześćdziesiąt klatek.

## 2. Liczba pisana po angielsku

Polski stawia przecinek tam, gdzie angielski kropkę, a `toFixed` stawia kropkę.
Każda ułamkowa liczba, jaką gracz kiedykolwiek przeczytał, była więc po angielsku
także w polskiej wersji: prędkość przez wodę, znos, działa strącone salwą,
szerokość geograficzna pod kursorem. **Jedenaście miejsc**, plus `kn` wpisane na
sztywno po angielsku w HUD bitwy obok przetłumaczonej etykiety.

Podręcznik gry wiedział o tym wcześniej: `help.ctrl_zoom` pisze `1,5×–12×`,
a `help.event_harvest_fx` `×0,6` — trzy wiersze nad `help.event_trade_boom_fx`,
które pisało `×1.5`. Jedna tabela, jeden rodzaj liczby, dwie pisownie.

`src/core/i18n/numbers.ts`: `fmtNum` (stały format) i `fmtTrim` (bez zer, dla
wskaźnika lunety). Świadomie **nie** `Intl.NumberFormat` — ten grupowałby tysiące
(`8 300` obok własnego `8300` gry), a reguła jest jedna.

Trzy bloki podręcznika bitwy (`battle.help_turn_body`, `_damage_body`,
`_reload_body`) **zostają z kropką**, z zapisanym powodem: to bloki **wzoru**,
`(1 − dRatio)^1.5` i tabela mnożników amunicji między kreskami, gdzie przecinek
czytałoby się jako separator listy. Strażnik ma na nie imienną listę i osobny
test, który pilnuje, żeby lista nie przeżyła wiersza, o którym mówi.

## 3. `1 Styczeń 1680` (TODO 8b)

Data to jedno zdanie, które gra pisze **w każdej klatce** — i **nie miało żadnego
testu**. `time.month_names` trzyma mianownik, a mianownik jest formą, której gra
nigdy nie drukuje: tabela ma **jednego** czytelnika (`getMonthName`), a każde
zdanie na niej zbudowane stawia przed miesiącem liczbę dnia, która po polsku żąda
dopełniacza. To ta sama wada, co porty w mianowniku z v0.69.0, w linijce
czytanej najczęściej.

Zdanie było napisane **trzy razy** — HUD przez `formatCalendarDate`, zakładka
kalendarza i lista zapisów na ekranie tytułowym — więc tę samą złą formę trzeba
było znaleźć w trzech plikach. Zakładka kalendarza nosi przy tym komentarz o
**roku**, który pomyliła z dokładnie tego samego powodu („jeden wywołujący
przekazuje argument, drugi nigdy się o nim nie dowiedział”), i kopia została.

Teraz buduje je `formatCalendarDay(day, startYear)`, formy dopełniacza stoją
w `plForms.ts` (a nie w `pl.ts`, bo `keys.test.ts` trzyma obie tabele locale na
tym samym zbiorze kluczy, a angielski nie ma czego postawić naprzeciw), i jest
strażnik na czwartą kopię oraz na drugiego czytelnika tabeli miesięcy.

Potwierdzone na ekranie: HUD i zakładka kalendarza czytają `1 stycznia 1680`,
etykiety portów `82,4°W 23,1°N`, HUD bitwy `Prędkość: 0,2 w.`

## 4. Stan

`tsc` czysty · **2475 testów w 92 plikach** · `audit-layout.mjs` **RAZEM: 0**
z 39 ekranów (168 s, 15 przeładowań) · `sweep-claims.mjs` 365 twierdzeń, 0
nieaktualnych · `sweep-constants.mjs` 176 liczb w 8 wymiarach.

Zakładka ustawień przestała zgłaszać `OBIECANE-NIEZWIAZANE: 8 9` — to był
skutek uboczny poprawki czytnika legend z v0.96.0 (wiersze `8 — Detale`).

## 5. Uwaga o sesji

Nad v0.96.0 i v0.97.0 pracowały **dwie sesje w jednym katalogu roboczym**
jednocześnie. Kosztowało to jedną cofniętą poprawkę (`pauseBehind`
w `MainMapScene`, słusznie — 8a nie odtwarza się) i jeden czerwony przebieg
suity z powodu pliku roboczego w `src/`, który `import.meta.glob` zdążył
wylistować, zanim druga sesja go skasowała. Reguła, która z tego wyszła:
**plik roboczy nie stoi w `src/`**, bo strażniki czytające źródła listują go
i przewracają się, gdy zniknie.

---

# v0.97.0.0 → v0.97.1.0 — magazyn TODO sprawdzony z kodem

Druga sesja („Pirates (2)”) wydała w międzyczasie v0.97.0.0 (daty w dopełniaczu,
przecinek dziesiętny, luneta). Ta część była robiona w osobnym worktree.

## Metoda

66 pozycji „znalezione, nienaprawione” (v0.71–v0.95) sprawdzonych z kodem jedna
po drugiej. **Sześć było już naprawionych i nieodhaczonych** — dla kolejnego
agenta to fałszywe tropy. Reszta: otwarte albo decyzje; odhaczone i opisane
w TODO.

## Co z tego wyszło

| | przed | po |
|---|---|---|
| Montserrat, Gwadelupa — szlak żywności | brak | Bermudy, Port Royal |
| szlaki na mapie | 78 | 80 (żaden inny się nie zmienił) |
| abordaż, który zdobywa statek | **dwa** `BattleEnded` (captured + surrender) | jeden |
| baner abordażu | kto wygrał | + straty obu stron |
| księga emerytalna, miasta/rodzina | bez liczby | z liczbą |

**Szlaki:** `TradeRouteSystem` sortował dostawców po czasie przeprawy z rabatem
tej samej korony ×0,7 i **dopiero potem** pytał o `MAX_LANE_LENGTH`. Francuskie
Florida Keys (1 546) wygrywały dla francuskich wysp i odpadały na zasięgu —
a zamiast drugiego w kolejce miasto dostawało **nic**. Test na prawdziwej mapie:
bez szlaku żywności zostaje wyłącznie Panamá.

**Podwójny koniec bitwy:** znaleziony przy weryfikacji banera na ekranie —
panel „PRZEJĘTO STATEK! +66” i pod nim „WRÓG SIĘ PODDAJE! +56”. Abordaż przerzedza
załogę poniżej 10, a to jest też reguła kapitulacji, sprawdzana w tym samym ticku.
Test: `["captured","surrender"]` na starym silniku, `["captured"]` na nowym.

## Pomiary bez decyzji (TODO pozycja 3)

- 18 z 69 notowań producentów na `RATIO_MIN` — ta sama przyczyna co złoto
  (`(rate || 1)` dla towaru, którego miasto nie je).
- Reputacja startowa stoi **na progach** (własna korona 20, piraci −20); w 12 z 84
  startów jeden kupiec sojusznika zamyka dom gubernatora.

---

# v0.97.1.0 → v0.97.2.0 — progi pogody, Toasty silnika

- **Toasty z silnika po angielsku.** `WorldEngine` wysyłał „Crew has gone
  ashore.” / „Crew has returned to ship.” jako gotowe napisy, obok kluczy
  `event.disembarked`/`event.embarked` wpisywanych dwie linie wyżej do dziennika.
  Strażnik w `no_hardcoded_text.test.ts` czyta teraz `src/core` (druga sesja
  rozszerza go na `src/game`).
- **Podłoga huraganu wracała stopień niżej** dla 22 z 400 wielkości płótna
  (`43 × 0,4 / 43 = 0,39999…`). Klasyfikacja stopni ma tolerancję 1e-9.
- **Szkwał = wiatr 1,0**, nie „+0,3”. Zachowanie zostaje (zmiana odczucia wymaga
  playtestu), zdania poprawione, pomiar przypięty. Decyzja w TODO, pozycja 3.
- **Instrukcja**: szkwał rwie płótno także przy połowie żagli (nieaktualne od
  v0.92.0).
- **Własne miasto stracone poza ekranem** — zamknięte pomiarem z kodu: dwie drogi
  zmiany właściciela, obie ogłaszane (dziennik + Toast).

---

# v0.97.2.0 → v0.98.0.0 — zbudowane i nigdy niezapytane

Wejście: pytanie, które zostawiła v0.97.0.0. `CameraController.setZoom` **nie
miał żadnego wywołania**, i był czwartą drogą do zoomu — czyli powodem, dla
którego ustawienie dawało się cofnąć. Ile jeszcze takich rzeczy stoi w
`src/game/`? Odpowiedź: **siedem metod publicznych i cztery całe klasy**. Jedna
z nich okazała się funkcją, której gracz nigdy nie dostał.

## 1. Siatka, którą widział i której nie umiał odczytać

Czarta ma siatkę południków i równoleżników. **Linie są poprawne** i rysuje je
`CartographicGrid`. Trzynaście etykiet mówiących, **który** to stopień, było
rysowanych w przestrzeni świata i wszystkie w jednym miejscu: pięć szerokości
przy `x = MAP_W/2 − 100`, osiem długości przy `y = MAP_H/2 − 50`. Jeden krzyż
pośrodku morza 3200×2400.

Zmierzone na wszystkich 45 portach i na trzech stopniach zoomu, przy których
siatka w ogóle się rysuje (poniżej 3×):

| zoom | etykiet w kadrze | portów, które widzą choć jedną |
|---|---|---|
| 1,5× | 62 / 585 | 25 / 45 |
| 2,0× | 35 / 585 | 20 / 45 |
| 2,5× | 18 / 585 | 15 / 45 |
| **razem** | **115 / 1755 = 6,6%** | |

W tym samym czasie **191 linii siatki przecina kadr** przy najszerszym stopniu.
Widział linie i w 93,4% przypadków żadnej liczby, która by je nazwała.

**A funkcja, która to robi poprawnie, była napisana i nigdy nie wywołana.**
`UIOverlayScene.updateGridLabels` — pięćdziesiąt linii, kompletne: odwzorowanie
Merkatora, wygaszanie, przycinanie do krawędzi, ustawianie etykiet **przy
marginesie ekranu**, tak jak robi to mapa morska. Ta sama rodzina co
`CameraController.setZoom` z v0.97.0.0 i gorsza: tamto było czwartym wejściem do
czegoś, co działało, to jest funkcja, której nie było.

Nakłada się to na poprzednie wydanie: etykiety wchodzą pełną siłą poniżej 2×,
a **do v0.97.0.0 ekran ustawień nie umiał utrzymać kamery poniżej 6×**. Nawet
podpięte, byłyby niewidoczne przy każdym stopniu, który ten ekran potrafił dać.

Reguły wygaszania były oczywiście dwie: linie pełne poniżej 2,2 i znikające przy
3, etykiety poniżej 2 i znikające przy 3. Teraz jedna — `gridFade(zoom)` —
nałożona na linie i **podana** etykietom, więc nie ma gdzie postawić drugiej.

Potwierdzone na ekranie po poprawce: przy 2,5× trzy etykiety, `20°N` przy
`x = 6` (lewa krawędź), `80°W` i `85°W` przy `y = 700` (dół), zero etykiet w
przestrzeni świata.

Przy okazji: `CartographicGrid.ts` importował Phasera **jako wartość**, używając
go wyłącznie jako typu — więc jego czysta arytmetyka Merkatora była zamknięta za
przeglądarką i do zmierzenia siatki musiałem ją przepisać do skryptu. To kształt
z v0.91.0. Import jest teraz `import type`, a test czyta odwzorowanie wprost.

## 2. Zbudowane i nigdy niezapytane

| co | stan |
|---|---|
| `MainMapScene:257` | `new FxManager(this);` — wynik wyrzucany |
| `InputMapper.setSailLevel` | puste ciało pod komentarzem `Deprecated` |
| `MusicManager.getCurrent` | bez czytelnika |
| `CommandQueue.get length` | bez czytelnika |
| `UIOverlayScene.updateGridLabels` | patrz wyżej — **podpięte** |

Usunięte. Strażnik (`dead_code.test.ts`) trzyma odpowiedź na zerze i ma **imienną
listę wyjątków z powodem**, bo dwa z nich nie są śmieciem: `FxManager.spawnSplash`
i `spawnSmoke` są napisane i nikt ich nie woła, bo **`CombatEngine` nie emituje
żadnego zdarzenia dla chybienia** (`CombatEngine.ts:713` — „Miss: only cooldown
is set; no damage events"). Ekran nie ma z czego narysować pióropusza wody.
Podpięcie ich to zmiana tego, co bitwa **zgłasza**, a nie tego, jak rysuje —
więc idzie do TODO, nie po cichu do tego wydania.

Cztery klasy nigdy nie budowane zostają na dysku, z zapisanym powodem:
`MinimapRenderer` (minimapa usunięta w v0.9.2), `ShoreWaveRenderer`
(udokumentowany eksperyment, sześć nieudanych podejść), `DOMCloudOverlay`
i `WindCompassRenderer` (zastąpione przez `CloudRenderer` i
`WindCompassWidget`). Kasowanie ich wyrzuciłoby pracę, o której użytkownik
może jeszcze zdecydować.

## 3. Legendą czarty jest podręcznik (TODO 8, otwarte od v0.84.0)

`MainMapScene` wiąże **osiemnaście klawiszy i nie nazywa na ekranie ani jednego**.
Wyjątek w `scene_legend.test.ts` tłumaczy to od v0.84.0 zdaniem, że jego klawisze
„są całą pierwszą stroną `HelpScene`" — **twierdzeniem o dwóch plikach, którego
nic nie sprawdzało**.

Zmierzone: tabela podręcznika i wiązania czarty to **te same osiemnaście
klawiszy, co do jednego**. Więc odpowiedzią na TODO 8 jest test, a nie nowy panel.

Jedna pułapka po drodze: skan źródła widział **dziesięć** klawiszy, nie osiemnaście.
Pozostałe osiem — ster i żagle — są **trzymane**, a nie naciskane, więc
`InputMapper` bierze je przez `addKey` i czyta `isDown` co klatkę. Skan szukający
`keydown-` nazwałby osiem wierszy podręcznika zmyślonymi.

Przy okazji, z podpowiedzi drugiej sesji: klawisz `V` wypisywał `Fog of war: ON`
/ `OFF (test)` — jedyny toast na tym ekranie, który nigdy nie przeszedł przez
`t()`. Strażnik toastów czyta teraz całe `src/`, nie samo `src/core`.


## 3a. Przełącznik z czterema czytelnikami i dwiema odpowiedziami

Trop od drugiej sesji, która przechodziła podręcznik wiersz po wierszu:
`V` (`help.ctrl_vision`) przestawia `worldRenderer.fogOfWarEnabled`, a `update()`
wylicza to pole **co klatkę** z ustawień — więc przełączenie żyje jedną klatkę.
Po sprawdzeniu okazało się większe.

`pc_debug` miał **czterech czytelników i dwie odpowiedzi** na przypadek, który
decyduje — **klucz nieustawiony**, czyli świeży profil:

| czytelnik | nieustawiony znaczył |
|---|---|
| `DebugSetting.isDebugMode()` — `=== "1"` | wyłączony |
| `PortMarkerRenderer.ts:142` — `=== "1"` | wyłączony |
| `OptionsMenuScene.ts:1341` — `raw === null ? true` | **włączony** |
| `MainMapScene.ts:1197` — `!== "0"` | **włączony** |

A czarta robiła co klatkę `fogOfWarEnabled = debugMode ? false : fogSetting`.
Czyli **na nowej instalacji mgła wojny nie mogła pojawić się w ogóle**:
przełącznik `Zasięg lunety` zapisywał `pc_fog`, a następna klatka to wyrzucała;
`V` było cofane, zanim zdążyło się narysować — i **toast był całym widocznym
działaniem tego klawisza**. Jedyna droga do funkcji prowadziła przez przełączenie
Debug w opcjach, bo tylko to zapisuje dosłowne `"0"`, którego potrzebuje
`!== "0"`. Przy okazji: świeży profil widział czerwone `Debug: ON`, podczas gdy
moduł, który ten przełącznik posiada, mówił „wyłączony".

To ten sam kształt co luneta z v0.97.0.0 — **ustawienie zapisane przez ekran
i zabrane przez pętlę klatki** — i to samo lekarstwo. `FogSetting.ts` posiada
`pc_fog` i odpowiada na dwa różne pytania: `isFogEnabled()` — o co poprosił
gracz, `fogOfWarActive()` — co czarta rysuje. Debug dalej odsłania mapę, ale
**mówi o tym** (`map.fog_debug`) zamiast udawać zepsute ustawienie.

Zmierzone w działającej grze, trzydzieści klatek po każdej zmianie:

| przypadek | `pc_fog` | `pc_debug` | mgła rysowana |
|---|---|---|---|
| świeży profil | — | — | nie |
| gracz włącza | `1` | — | **tak** |
| włączona, debug też | `1` | `1` | nie |
| włączona, debug wyłączony | `1` | `0` | tak |

Drugi wiersz był przed poprawką niemożliwy — `fogDrawn` było `false` we
wszystkich czterech.

Przy okazji cztery angielskie literały na ekranie ustawień (`Debug: ON/OFF`,
`Spyglass range: ON/OFF`) — niewidoczne dla przemiatania szukającego polskich
znaków, dokładnie ta pułapka, którą opisuje komentarz w `OptionsMenuScene:965`.
I `?debug=` w `PreloadScene` wpisywał do klucza **surową wartość z adresu**, więc
`?debug=yes` zapisywało `yes`, czytane inaczej przez każdego z czterech.

## 4. Stan

`tsc` czysty · **2514 testów w 95 plikach** · `audit-layout.mjs` **RAZEM: 0**
z 39 ekranów (156 s, 14 przeładowań) · `sweep-claims.mjs` 366 twierdzeń, 0
nieaktualnych · `sweep-constants.mjs` 176 liczb.

## 5. Nowe, nienaprawione

- **Chybienie nie jest zdarzeniem.** `CombatEngine` przy pudle ustawia tylko
  przeładowanie. `FxManager.spawnSplash` i `spawnSmoke` czekają na zdarzenie,
  którego nie ma. Do rozważenia razem, bo to zmiana w tym, co silnik zgłasza.
- **Progi zoomu są rozsypane po `src/game/render/`** — 2, 2,2, 3, 4 w
  `MapEventMarkerRenderer`, `MountainRenderer`, `CirrusRenderer`,
  `SeagullRenderer`, `ShoreWaveRenderer` i dwa w `MainMapScene`. Każdy o czym
  innym, więc to nie jest jedna liczba do scalenia — ale **komentarz
  `MountainRenderer` mówi „full opacity at zoom >= 4", a kod czyta
  `zoom < 2 ? 0.5 : 1`**. `sweep-claims.mjs` czyta tylko `core/`, więc takie
  zdania w `src/game/` nie są przez nic sprawdzane. To jest pozycja: **rozszerzyć
  przemiatanie twierdzeń na warstwę rysującą.**

# v0.98.0.0 → v0.98.1.0 — podręcznik przeczytany drugi raz, zegar w porcie zmierzony

## Podręcznik w grze

Ostatnie czytanie wiersz po wierszu: v0.66.0. Od tamtej pory reguły zmieniły
się trzydzieści razy. Subagent przeczytał 151 kluczy `help.*`/`battle.help_*`
z kodem (obliczenia polaru i obrażeń uruchamiając kod, nie z pamięci); każde
znalezisko sprawdzone przed poprawką.

| klucz | było | jest |
|---|---|---|
| `battle.help_range_body` | pół ekranu, ±60° od prostopadłej | ćwierć ekranu (`arena/12`), ±30° (`BROADSIDE_ARC_COS` 0,5) |
| `battle.help_sails_body` | uszkodzenia liniowo | progi ×0,75 / ×0,45 / 0, kadłub ×0,88 / ×0,70 / ×0,45 |
| `battle.help_examples_body` | 35/10/15, 110, 18 salw | 32/15/21, 160, 20 salw |
| `battle.help_boarding_body` | siła załóg rozstrzyga | pojedynek rozstrzyga, siła załóg liczy straty |
| `battle.help_damage_body` | celność bez kanonierstwa | × (0,85 + 0,03·kanonierstwo) |
| `battle.help_reload_body` | morale spada, gdy giną ludzie | przy każdym trafieniu, od kartacza 4× |
| `help.econ_prices_b2` | magazyn = poziom × 50 | 8 dni własnych zbiorów, 20–96 t (test) |
| `help.econ_wealth_b` | sprzedaż podnosi bogactwo | zakupy zasilają kasę, sprzedaż z niej bierze |
| `help.econ_can_b` | złoto = szlak skarbowy | miasto wydobywa złoto i się bogaci |
| `help.event_treasure_fleet_fx` | Vera Cruz → Hawana | Vera Cruz albo Stały Ląd → Hawana |
| `help.event_war_fx` | ×2 okrętów | więcej okrętów, efekt słabnie przez 2 lata |
| `help.sail_*` (5) | 150%, 90-110%, 6-7×/3×, 55-60°, „połowę dłużej” | 125% przy pasacie, 95-105%, 9×/4×, 50-60°, ×1,6/×2,4/×3,5 |
| `help.ctrl_land` | zejdź na ląd / wróć | tylko powrót; na ląd schodzisz, wpływając na brzeg |
| `battle.help_controls_body` | ESC — wycofaj się | …dopiero poza 90% zasięgu dział (test na `DISENGAGE_RANGE_MUL`) |
| `help.event_famine_fx` | żywność ×2, woda ×2 | ×2, potem drożej |

„Głód: 2–4× cena” (`help.econ_can_b`) okazało się **prawdziwe**: osiadły świat,
45 miast, 1,75–2,17× pierwszego dnia i 1,75–3,83× dziewięćdziesiątego. Teraz
pilnuje go test w `producer_shed.test.ts`.

`help.ctrl_vision` (`V`) nie jest wadą tekstu, tylko kodu — cztery miejsca
czytają `pc_debug` na dwa sposoby i mgła wojny nie może się pojawić nowemu
graczowi; przejęte przez drugą sesję (v0.98.0.0).

## Zegar w porcie

Pomiar bez zmiany kodu, decyzja w TODO: wizyta w porcie kosztuje 0 czasu gry
z konstrukcji, a każdy termin w dniach (kontrakty, zlecenia, dzierżawa, łup,
wojny, wyprawy) liczy wyłącznie dni żeglugi. Zdanie do podręcznika w grze
**nie** zostało dopisane — utrwaliłoby przypadek jako regułę bez decyzji.
